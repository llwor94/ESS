import React, { useState } from 'react';


function Contact({styles}) {
  return (
    <div className={`${styles.section} ${styles.contact}`}>
      <h2>Contact us</h2>
    </div>
  )
}

export {Contact}
